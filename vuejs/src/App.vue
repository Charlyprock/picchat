<template>
    <router-view/>
</template>


<script>
    
    export default{
        
        data(){
            return{

            }
            
        },

        created(){

        },

        async mounted(){
            // var data = "maman"
            // const key = await this.$fonct.generate_key()
            // const key = await this.$fonct.import_key("ma_cles")
            // const enc = await this.$fonct.encrypt(data, "ma_cles")

            // const decrypt = await this.$fonct.decrypt(new Uint8Array(JSON.parse(enc).encrypt), new Uint8Array(JSON.parse(enc).iv), "ma_cles")

            // console.log("decoder app", decrypt)

        },

        methods: {

        }
        

        
    }
</script>